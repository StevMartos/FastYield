# sim_data

This directory is intentionally not tracked in Git.

Download the full FastYield `sim_data/` directory from the FileSender link provided in the main README, then define:

export FASTYIELD_SIM_DATA_PATH="/path/to/sim_data"
